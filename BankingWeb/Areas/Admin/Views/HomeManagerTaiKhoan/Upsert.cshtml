@model CardVM

<div class="card-body">
    @*<h4 class="card-title text-dark text-uppercase text-center">@(Model.TaiKhoan.Id == Guid.Empty ? "Update" : "Create") Card </h4>*@
    <form class="forms-sample" method="post">
        <input asp-for="TaiKhoan.Id" hidden />
        <div class="form-group">
            <label asp-for="TaiKhoan.AccountNumber"></label>
            <input asp-for=TaiKhoan.AccountNumber type="text" class="form-control bg-white text-dark" placeholder="Name">
            <span class="text-danger " asp-validation-for="TaiKhoan.AccountNumber"></span>
        </div>
        <div class="form-group">
            <label asp-for="TaiKhoan.Balance"></label>
            <input asp-for=TaiKhoan.Balance type="text" class="form-control bg-white text-dark" placeholder="Name">
            <span class="text-danger " asp-validation-for="TaiKhoan.Balance"></span>
        </div>
        <div class="form-group">
            <label asp-for="TaiKhoan.AccountTypeId"></label>
            <select asp-for="TaiKhoan.AccountTypeId" asp-items="@Model.AccountType" class="form-select">
                <option disabled selected>--Select Account Type--</option>
            </select>
            <span asp-validation-for="TaiKhoan.AccountTypeId" class="text-danger"></span>
        </div>
@*        @if (Model.TaiKhoan.Id != Guid.Empty && Model.TaiKhoan.Id != null)
        {
            <button type="submit" class="btn btn-primary mr-2">Update</button>
        }
        else
        {*@
            <button type="submit" class="btn btn-primary mr-2">Create</button>
        @*}*@
        <a class="btn btn-dark" asp-action="Index" asp-controller="AccountType">Cancel</a>
    </form>
</div>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}